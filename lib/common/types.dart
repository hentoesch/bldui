enum DesignItemType {
  listColumn,
  listParagraph,
  listRow,
  Layout,
  formField,
}

enum FieldAction { addField, removeField, selectField, unselectField }

enum JsonTemplate {
  list,
  form,
  dataModelField,
  dataModelTable,
  listColumn,
  formField;
}

enum UIdesignerType {
  list,
  form,
}
